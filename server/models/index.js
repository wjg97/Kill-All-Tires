const User = require('./User');
const {Vehicle} = require('./Vehicle');
const Appointment = require('./Appointment');

// Export an object containing all of our models
module.exports = { User, Vehicle, Appointment };